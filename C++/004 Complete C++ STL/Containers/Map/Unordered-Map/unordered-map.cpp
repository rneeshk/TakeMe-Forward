#include<bits/stdc++.h>
using namespace std;

/*
    unordered_map: Everything same as map, but key are not stored in
    sorted order.

    unordered_map - O(1) in average TC
    O(n) in very rarely worst case
*/
void explainMultiMap(){

        /* 
            All functions are same as map erase, swap, size, empty, 
            are same as above
        */

}
int main()
{
    explainMultiMap();
    return 0;
}