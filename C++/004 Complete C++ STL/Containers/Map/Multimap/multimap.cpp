#include<bits/stdc++.h>
using namespace std;

/*
    Multimap: Everything same as map, only it can store multiple 
    duplicates key, only mpp[key] cannot use here.
*/
void explainMultiMap(){

        /* 
            All functions are same as map erase, swap, size, empty, 
            are same as above
        */

}
int main()
{
    explainMultiMap();
    return 0;
}