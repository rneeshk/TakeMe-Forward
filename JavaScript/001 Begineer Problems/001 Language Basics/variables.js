/*

    Variables : Variables are container for data. For ex: Home Grocery Boxes.  
    
    Memory Location 

    Dynamically Type Language: Javascript can hold values with different data types 
    in the same variable during runtime

*/

age = 24;
name = "Rajneesh";
price = 150.50;
value = null;
notKnown = undefined;
isFollow = true;
console.log(age, name, )

/*
    var : Variables can be re-declared and update. A global scope variable
    let : Variables cannot be re-declared and update. A block scope variable
    const : Variable cannot be re-declared or update. A block scope variable
*/

