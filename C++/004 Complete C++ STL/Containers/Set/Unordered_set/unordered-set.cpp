#include<bits/stdc++.h>
using namespace std;

/*
    Unordered_set: It stores unique element in unsorted order.
    lower_bound() and upper_bound() function doesn't work.
    Rest all function are same as above.
    It does not stores in any particular order it, it has a better 
    complexity that set in most cases, except some when collision happen

    most of the operation takes O(1) T.C and worst case O(n) but worst case 
    doesn't happen usually.
*/
void explainUnorderedSet(){

}
int main()
{
    explainUnorderedSet();
    return 0;
}